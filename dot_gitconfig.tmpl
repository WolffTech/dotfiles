{{- $gh := lookPath "gh" -}}
[user]
	name = Nick Wolff
	email = nick@wolff.tech
[credential]
	helper = cache
{{ if $gh }}
[credential "https://github.com"]
	helper =
	helper = !{{ $gh }} auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !{{ $gh }} auth git-credential
{{ end }}
[core]
	editor = nvim
	excludesfile = {{ .chezmoi.homeDir }}/.gitignore_global
