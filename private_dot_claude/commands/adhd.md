---
description: Rewrite one response in a concise, ADHD-friendly format
---

Load the `adhd` skill and apply it to this response only.

If `$ARGUMENTS` contains a request, answer that request using the skill.

If `$ARGUMENTS` is empty, rewrite the most recent assistant response using the skill. Return only the replacement response, without explaining that it was rewritten.

$ARGUMENTS
